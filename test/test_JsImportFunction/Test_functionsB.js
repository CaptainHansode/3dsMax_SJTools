/**---------------------------------------------------------------
 * テスト関数
 * @param {float} x
 * @param {float} y
 * @return {float}
 */
function testCal(x, y)
{
    var x = x || 0.0;
    var y = y || 0.0;
    return x + y;
}

